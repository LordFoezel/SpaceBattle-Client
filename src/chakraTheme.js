import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: '"Black Ops One", Segoe UI, Tahoma, Geneva, Verdana, sans-serif',
    body: '"Black Ops One", Segoe UI, Tahoma, Geneva, Verdana, sans-serif',
  },
});

export default theme;

